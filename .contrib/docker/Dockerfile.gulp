FROM node:24
RUN npm --version
WORKDIR /app
VOLUME /app
CMD ["bash","-c", "npm ci && npm rebuild node-sass && npx gulp build"]
#CMD ["bash","-c", " npm ci ; npm rebuild node-sass ; npx gulp build ;sleep infinity "]
